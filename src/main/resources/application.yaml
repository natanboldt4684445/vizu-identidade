spring:
  application:
    name: indentidade

server:
  port: 8082
  servlet:
    context-path: '/indentidade/'

eureka:
  client:
    eureka-service-url:
      defaultZone: 'http://localhost:8761/eureka'
    instance:
      hostname: localhost
